{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "SCSS",
            "type": "shell",
            "[disable]command": "sass --watch scss:css",
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}

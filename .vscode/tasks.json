{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "SCSS",
            "type": "shell",
            "command": "sass --watch scss:css",
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}
